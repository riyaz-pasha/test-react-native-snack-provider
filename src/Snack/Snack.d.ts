/// <reference types="react" />
import { FullSnackConfig } from '../Types';
export declare const Snack: ({ message, subMessage, onClose, onPress, id, duration, hideIcon, iconName, iconFamily, iconColor, backgroundColor, closeButtonBackgroundColor, closeIconColor, snackType, successColor, errorColor, infoColor, }: FullSnackConfig) => JSX.Element;
export default Snack;
